jdk:
  - openjdk17

android:
  components:
    - platform-tools
    - build-tools-35.0.0
    - android-35

# Use standard JitPack approach
install:
  - ./gradlew clean publishToMavenLocal -x test -x lint